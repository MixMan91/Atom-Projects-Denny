//let total = 0;

//for (let i = 0; i < numbers.length; i++) {
  //total += numbers[i];
//}

//Another way it can be written

//let total = 0;

//numbers.forEach(function(one_number) {
  //total += one_number;
//});
